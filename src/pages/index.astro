---
import Hero from "@/components/Home/Hero";
import WorkExperience from "@/components/Home/WorkExperience";
import Education from "@/components/Home/Education";
import Projects from "@/components/Home/Projects";
import Journey from "@/components/Home/Journey";
import Layout from "../layouts/Layout.astro";
import { heroData } from "@/data/hero";
import { workData } from "@/data/workExperience";
import { educationData } from "@/data/education";
import { projectData } from "@/data/projects";
import { journeyData } from "@/data/journey";
---

<Layout>
  <Hero
    client:visible
    phrases={heroData.phrases}
    description={heroData.description}
  />
  <WorkExperience client:visible experiences={workData} />
  <Education client:visible experiences={educationData} />
  <Projects client:visible projects={projectData} />
  <Journey client:visible journeyData={journeyData} />
</Layout>
